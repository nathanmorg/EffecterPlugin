name: Effecter
version: '${project.version}'
main: n8.effecter.Effecter
api-version: 1.19
prefix: Effecter
authors: [ n8 ]
description: Effecter gives random negative or positive effect to players which change overtime based on event.
website: github.com/nathanmorg
commands:
  createeffect:
    description: Creates a new effect for players logging in.
    usage: /createeffect
    aliases: /effectcreate
    permission: create
    permission-message: You do not have permission to execute this command.
